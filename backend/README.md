# FindPros Backend

Express API for categories and tasks.

## Setup

1. Install dependencies:

   ```bash
   npm install
   ```

2. Create `.env` from `.env.example` and update `MONGO_URI` if needed.

3. Start MongoDB locally.

4. Run the API:

   ```bash
   npm run dev
   ```

The API runs at `http://localhost:5000`.
